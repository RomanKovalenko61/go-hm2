# Go REST API with Docker

Мини-приложение REST API на Go с метриками Prometheus.

## Структура проекта

- **app** - основное Go приложение (REST API для управления пользователями)
- **prometheus** - система мониторинга метрик
- **grafana** - визуализация метрик (опционально)

## Быстрый старт

### Использование Makefile (рекомендуется)

```bash
# Показать все доступные команды
make help

# Запустить все сервисы
make docker-up

# Посмотреть логи
make docker-logs

# Остановить сервисы
make docker-down

# Пересобрать и перезапустить
make docker-restart

# Создать тестового пользователя
make create-user

# Получить всех пользователей
make get-users
```

### Запуск с Docker Compose

```bash
# Сборка и запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down

# Остановка и удаление volumes
docker-compose down -v
```

### Запуск только приложения

```bash
# Сборка образа
docker build -t go-hm2-app .

# Запуск контейнера
docker run -d -p 8080:8080 --name go-hm2 go-hm2-app

# Просмотр логов
docker logs -f go-hm2

# Остановка контейнера
docker stop go-hm2

# Удаление контейнера
docker rm go-hm2
```

## Доступные сервисы

После запуска `docker-compose up -d`:

- **API**: http://localhost:8080/api
- **Метрики Prometheus**: http://localhost:8080/metrics
- **Prometheus UI**: http://localhost:9090
- **Grafana**: http://localhost:3000 (логин: admin, пароль: admin)

### 📊 Grafana Dashboard

Grafana автоматически настраивается при запуске с:
- ✅ Готовым источником данных Prometheus
- ✅ Преднастроенным дашбордом "Go API Metrics Dashboard"
- ✅ Автоматическим отображением метрик API

**Метрики на дашборде:**
- Общее количество запросов API
- Частота запросов в секунду
- Время отклика (латентность)
- Статус-коды HTTP
- Запросы по эндпоинтам
- Использование памяти
- Активные goroutines

Просто откройте http://localhost:3000 и войдите (admin/admin)!

## API Endpoints

### Пользователи

- `GET /api/users` - получить всех пользователей
- `POST /api/users` - создать пользователя
- `GET /api/users/{id}` - получить пользователя по ID
- `PUT /api/users/{id}` - обновить пользователя
- `DELETE /api/users/{id}` - удалить пользователя

### Примеры запросов

```bash
# Создать пользователя
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"John Doe","email":"john@example.com"}'

# Получить всех пользователей
curl http://localhost:8080/api/users

# Получить пользователя по ID
curl http://localhost:8080/api/users/1

# Обновить пользователя
curl -X PUT http://localhost:8080/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"Jane Doe","email":"jane@example.com"}'

# Удалить пользователя
curl -X DELETE http://localhost:8080/api/users/1

# Посмотреть метрики
curl http://localhost:8080/metrics
```

## Мониторинг

### Prometheus

Откройте http://localhost:9090 для доступа к Prometheus UI.

Примеры запросов:
- `http_requests_total` - общее количество HTTP запросов
- `http_request_duration_seconds` - длительность запросов

### Grafana

1. Откройте http://localhost:3000
2. Войдите с логином `admin` и паролем `admin`
3. Добавьте Prometheus как источник данных:
   - URL: `http://prometheus:9090`
4. Создайте дашборд для визуализации метрик

## Разработка

### Локальный запуск без Docker

```bash
# Установка зависимостей
go mod download

# Запуск приложения
go run main.go
```

### Пересборка образа

```bash
# Пересобрать и перезапустить
docker-compose up -d --build
```

## Технологии

- Go 1.24.2
- Gorilla Mux - HTTP роутер
- Prometheus - метрики
- Docker & Docker Compose

