# Быстрый старт

## Запуск приложения

```bash
# 1. Запустить все сервисы
make docker-up

# 2. Проверить статус
make docker-ps

# 3. Создать тестового пользователя
make create-user

# 4. Получить всех пользователей
make get-users

# 5. Посмотреть логи
make docker-logs

# 6. Остановить сервисы
make docker-down
```

## Доступные сервисы

- **API**: http://localhost:8080/api
- **Метрики**: http://localhost:8080/metrics
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (логин: admin, пароль: admin)

### Grafana Dashboard

После запуска `make docker-up` Grafana автоматически настраивается с:
- ✅ Преднастроенным источником данных Prometheus
- ✅ Готовым дашбордом "Go API Metrics Dashboard"
- ✅ Автоматическим обновлением метрик каждые 5 секунд

**Дашборд показывает:**
- 📊 Счетчик общего количества API запросов
- 📈 График частоты запросов (requests/sec)
- ⏱️ Время ответа API (50-й и 95-й процентили)
- 🎯 Распределение запросов по HTTP статус-кодам
- 🔍 Запросы по конкретным эндпоинтам
- 💾 Использование памяти приложением
- 🔄 Количество активных goroutines

Просто откройте http://localhost:3000 и войдите (admin/admin) - дашборд откроется автоматически!

## Команды Makefile

```bash
make help              # Показать все доступные команды
make docker-up         # Запустить все сервисы
make docker-down       # Остановить сервисы
make docker-restart    # Пересобрать и перезапустить
make docker-logs       # Показать логи
make create-user       # Создать пользователя
make get-users         # Получить пользователей
make get-metrics       # Посмотреть метрики
make generate-metrics  # Сгенерировать тестовые метрики для Grafana
```

## API Examples

```bash
# Создать пользователя
curl -X POST http://localhost:8080/api/users \
  -H "Content-Type: application/json" \
  -d '{"name":"John","email":"john@example.com"}'

# Получить всех пользователей
curl http://localhost:8080/api/users

# Получить пользователя по ID
curl http://localhost:8080/api/users/1

# Обновить пользователя
curl -X PUT http://localhost:8080/api/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"Jane","email":"jane@example.com"}'

# Удалить пользователя
curl -X DELETE http://localhost:8080/api/users/1
```

